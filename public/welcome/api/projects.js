export default function handler(req, res) {
  // =============================
  // PROJECT METADATA
  // GET    list projects by group
  // POST   create project (admin/facilitator)
  // PATCH  update status / metadata
  // =============================

  res.status(200).json([]);
}
